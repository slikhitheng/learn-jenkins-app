pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                deleteDir()
                checkout scm
                echo 'Hello World!!!!!!!!!'
            }
        }
    }
}
