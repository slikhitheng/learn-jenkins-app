pipeline {
    agent any

    environment {
        NETLIFY_SITE_ID = '0564507d-66f9-4c48-a744-453f6ac6afed'
        NETLIFY_AUTH_TOKEN = credentials('netlify-token')
    }

    stages {
        stage('build Docker image') {  // fixed typo here
            steps {
                sh '''
                    docker image build -t my-playwright .
                '''
            }
        }
    }
}
